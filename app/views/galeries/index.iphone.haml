.wrapper
	= render :partial => "layouts/top_menu_without_link"
	%p.clear
	
	.container_6		
		%p.clear
		-@galeries.each do |pic|
			.grid_2.product 
				=link_to image_tag(pic.img.url(:grid_2)), product_galery_path(pic.product.id, pic.id)