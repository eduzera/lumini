.wrapper
	= render :partial => "layouts/top_menu"
	.container_6
		.submenu
			%p.grid_2
				= link_to "#{t "categoria.por_tipo"}", categories_path, :class => "active"
			%p.grid_2 &nbsp;
			%p.grid_2
				= link_to "#{t "categoria.por_marca"}", manufactures_path, :class => "dissolve"
	
		%ul
			- @categories.each do |category|
				%li= link_to category.name, family_path(category), :class => "slide.in"
