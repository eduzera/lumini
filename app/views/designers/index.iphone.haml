.wrapper
	= render :partial => "layouts/top_menu_without_link"
	
	.container_6
		.designer
			%p.grid_6.title 
				= @designer.name
			%p.clear
			%p.grid_4.subtitle 
				= @designer.bio

			%p.grid_2.subtitle.photo 
				= image_tag (@designer.images.second.nil? ? @designer.images.first.img.url(:grid_2) : @designer.images.second.img.url(:grid_2))